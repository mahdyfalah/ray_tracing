use ray_tracing::service::scene_import_service::SceneImportService;

fn main() {
    // match SceneImportService::import_scene() {
    //     Some(scene) => {
    //         println!("Scene imported successfully: {:?}", scene);
    //     }
    //     None => {
    //         println!("Failed to import the scene.");
    //     }
    // }
}
